<div class="container-fluid">
  <div class="container pb-3 pb-lg-5">
    <div class="content">
      <h1 class="display-1 mt-3 mt-lg-5">
        {% if entry.title %}
          {{ entry.title }}
        {% else %}
          {{ entry.owner.title }}
        {% endif %}
      </h1>
    </div>
  </div>
</div>